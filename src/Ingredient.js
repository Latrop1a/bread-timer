class ingredient {
  constructor(type, name, description, unit, allergyInfo) {
    this.type = type;
    this.name = name;
    this.description = description;
    this.unit = unit;
    this.allergyInfo = allergyInfo;
  }
  pushArray();
}

//ingredient list with names, category and picture

